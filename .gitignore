# =============================================================================
# Unreal Engine 5 Project .gitignore
# Project: Leaves
# =============================================================================

# -----------------------------------------------------------------------------
# UE5 generated / build output directories（不入库）
# -----------------------------------------------------------------------------
Binaries/
DerivedDataCache/
Intermediate/
Saved/
Build/

# 插件下的同名目录也忽略
Plugins/**/Binaries/
Plugins/**/Intermediate/
Plugins/**/DerivedDataCache/

# -----------------------------------------------------------------------------
# IDE / 编辑器生成文件
# -----------------------------------------------------------------------------
# Visual Studio
.vs/
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
*.vcxproj
*.vcxproj.filters
*.vcxproj.user

# Rider / JetBrains
.idea/
*.iml

# VS Code
.vscode/

# CodeBuddy / Coding Copilot 本地缓存（保留 plan 等元数据由用户决定，这里默认忽略）
.codebuddy/

# Xcode（跨平台备用）
*.xcodeproj/
*.xcworkspace/
DerivedData/

# -----------------------------------------------------------------------------
# UE 项目文件（部分入库 / 部分忽略）
# -----------------------------------------------------------------------------
# .vsconfig 由 UE 自动生成，可忽略
.vsconfig

# Makefile（Linux 构建产物）
Makefile

# 项目 .uproject 是必须入库的（不要 ignore）
# Source/ Config/ Content/ 全部入库

# -----------------------------------------------------------------------------
# 编辑器临时 / 备份文件
# -----------------------------------------------------------------------------
*.tmp
*.temp
*.bak
*.autosave
*.swp
*~

# UE 资产备份
*.uasset.bak
*.umap.bak

# -----------------------------------------------------------------------------
# OS 系统文件
# -----------------------------------------------------------------------------
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Linux
*~
.fuse_hidden*
.Trash-*

# -----------------------------------------------------------------------------
# 日志 / 崩溃报告
# -----------------------------------------------------------------------------
*.log
*.pdb
*.dmp

# -----------------------------------------------------------------------------
# 大文件 / 二进制（建议走 Git LFS，下面只是兜底）
# 如果你已经配置 LFS，可以删掉这一段
# -----------------------------------------------------------------------------
# *.uasset  filter=lfs
# *.umap    filter=lfs
# *.fbx     filter=lfs
# *.wav     filter=lfs
# *.mp3     filter=lfs
# *.png     filter=lfs

# -----------------------------------------------------------------------------
# 用户私有 / 本地配置（不要入库）
# -----------------------------------------------------------------------------
# UE 编辑器布局 / 个人偏好
Config/DefaultEditorPerProjectUserSettings.ini
*.user
